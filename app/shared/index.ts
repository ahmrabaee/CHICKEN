// Shared types, schemas, and constants
// Export your shared types from this file

export interface AppConfig {
    version: string;
    name: string;
}

export const APP_CONFIG: AppConfig = {
    version: "0.1.0",
    name: "Financial Management Program",
};
